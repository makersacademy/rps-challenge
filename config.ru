require_relative 'app.rb'
run Rps

# Can be used to run the web app directly
# From the root of the folder call:
# rackup -p 4567

# This starts the web server on port 4567
# So on a browser the webpage can be accessed:
# http://localhost:4567
