<!DOCTYPE html>
<html lang="en">
<head>
  <title>Rock, Paper, Scissors</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="description" content="" />
</head>

<body>
  <h1>Rock, Paper, Scissors</h1>

  <div id="player1">
    <p><%=session[:player].name%> has chosen <%=session[:move]%></p>
  </div>

  <div id="player2">
    <p><%=session[:opponent].name%> has chosen <%=session[:opponent].choice%></p>
  </div>

  <div id="result">
    <%if @game.winner == :draw %>
      <p>It's a draw</p>
    <%else%>
      <p>The winner is <%=@game.winner.name%>  </p>
    <%end%>
  </div>
</body>
</html>
