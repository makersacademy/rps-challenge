<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rock Paper Scissors!</title>
    <link rel="stylesheet" href="/css/play.css">
  </head>
  <body>
    <div class="center">
      <h1>Welcome to the arena <%= @game.player_one.name %> and <%= @game.player_two.name %></h1>
      <br><br>
      <div class="battle_display">
        <div class="p1_display">
          <h1><%= @game.player_one.name %> chose <%= @game.player_one.choice %>!</h1><br>
          <img src="/<%= @game.player_one.image %>">
        </div>
        <div class="p2_display">
          <h1><%= @game.player_two.name %> chose <%= @game.player_two.choice%>!</h1><br>
          <img src="/<%= @game.player_two.image %>" alt="">
        </div>
      </div>
      <% if @game.result == :p1win %>
      <span class="winner"><%= @game.player_one.name %> wins!!</span>
      <% elsif @game.result == :p2win %>
      <span class="winner"><%= @game.player_two.name %> wins!!</span>
      <% else %>
      <span class="winner">Its a draw!!</span>
      <% end %>
    </div>
    <a id="play_again" href="/">Play again!</a>
  </body>
</html>
