<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rock Paper Scissors!</title>
    <link rel="stylesheet" href="/css/play.css">
  </head>
  <body>
    <div class="center">
      <h1>Welcome to the arena <%= @game.player_one.name %></h1>
      <div class="battle_display">
        <div class="p1_display">
          <h1><%= @game.player_one.name %>, you chose <%= @game.player_one.choice %>!</h1>
          <img src="/<%= @game.player_one.image %>">
        </div>
        <div class="p2_display">
          <h1>CPU chose <%= @game.cpu.choice%>!</h1>
          <img src="/<%= @game.cpu.image %>" alt="">
        </div>
      </div>
      <% if @game.result == :win %>
      <span class="winner"><%= @game.cpu.name %> wins!!</span>
      <% elsif @game.result == :lose %>
      <span class="winner"><%= @game.player_one.name %> wins!!</span>
      <% else %>
      <span class="winner">Its a draw!!</span>
      <% end %>
    </div>
  </body>
</html>
