<!DOCTYPE html>
<html>
<head >
  <style media="screen">
    .message {
      font-size: 5em;
      color: #BADA55;
    }
    body {
      font-family: "Helvetica Neue",Arial,Helvetica,sans-serif;
      color: #333;
      background: #F0F0F0;
      line-height: 1.5;
      margin: 0 auto;
      text-align: center;
    }
    input[type=submit] {
      cursor: pointer;
      background-color: #2A2025;
      font-size: 1.2em;
      color: #fff;
      font-weight: 700;
      border-radius: 10px;
      margin: 1em;
      padding: .8em 2em;
    }
    div[name=result] {
      color: #702020;
    }
    span[name=move] {
       width: 25%;
    }
  </style>
</head>

<body>
<span class="message"> <%= @game.player1.name %> VS <%= @game.player2.name %> </span>

<form action=/choice method=post >
  <input type=submit name=move value=Rock>
  <input type=submit name=move value=Paper>
  <input type=submit name=move value=Scissors>
</form>

<% unless @game.player1.choice.nil? %>
  <div name=moves>
  <span style="float:left;" class="message" name=move ><%= @game.player1.name %> chose <%= @game.player1.choice%>! </span>
  <span style="float:right;" class="message" name=move ><%= @game.player2.name %> chose <%= @game.player2.choice%>! </span>
  </div>

<div class=message name=result>
  <span type=text > <%= @game.result(@game.player1.choice, @game.player2.choice) %></span>
</div>

<% end %>

</body>
</html>
