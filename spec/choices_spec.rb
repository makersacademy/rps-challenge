require 'choices'

describe Choices do
  context("constants ") do
    let(:choices) {Choices.new}
    it("CHOICES[:scissors] is 0") do
      expect(Choices::CHOICES[:scissors]).to eq 0
    end

    it("CLASSIC constant") do
      let(:ch)
    end
  end

end
