<h1>
  <%=@player1_name%> chose <%= @game.player1_choice.to_s.upcase %>!
  <% if !@player2_name %>Computer<% else %><%=@player2_name%><% end %> chose <%= @game.player2_choice.to_s.upcase %>!
</h1>

<h2>
  <% if @game.winner == :draw %>
    This round is a draw...
  <% elsif @game.winner == :win %>
    <%=@player1_name%> wins!
  <% elsif @game.winner == :lose %>
    <% if !@player2_name %>Computer<% else %><%=@player2_name%><%end%> wins!
  <% else %>
    What's going on?
  <% end %>
</h2>
